version: '3.5' 

# Define the services/containers to be run
services:
  angular-service:
    container_name: angular-container
    build: ./ 
    volumes: 
    - './:/usr/src/app'
    ports:
      - "4200:4200" 




